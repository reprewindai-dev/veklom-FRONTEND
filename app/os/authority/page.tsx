import { WorkspaceScaffold } from "@/components/cos/WorkspaceScaffold";
export default function AuthorityPage() { return <WorkspaceScaffold stage="Authority" title="Authority" description="Inspect key identifiers, permissions, caps, leases, and revocation state without exposing private keys." />; }
