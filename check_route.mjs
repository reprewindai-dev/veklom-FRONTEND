import fs from 'fs';
console.log(fs.readFileSync('app/api/[...proxy]/route.ts', 'utf8').split('\n').filter(l => l.includes('identity') || l.includes('workspace') || l.includes('path.startsWith')).join('\n'));