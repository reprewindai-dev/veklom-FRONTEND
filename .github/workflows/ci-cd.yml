name: veklom-FRONTEND CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: frontend-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Next.js Build Check
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: PowerShell script syntax check
        shell: pwsh
        run: |
          $failed = $false
          Get-ChildItem -Path scripts -Filter *.ps1 -File | ForEach-Object {
            $tokens = $null
            $errors = $null
            [void][System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$tokens, [ref]$errors)
            if ($errors.Count -gt 0) {
              $failed = $true
              Write-Host "PowerShell parse errors in $($_.FullName):"
              $errors | ForEach-Object { Write-Host "  $($_.Message)" }
            }
          }
          if ($failed) { exit 1 }

      - name: Lint check
        run: npm run lint

      - name: Type check
        run: npm run typecheck

      - name: Unit tests
        run: npm test -- --ci

      - name: Production build
        run: npm run build

      - name: Report build result
        if: success()
        run: echo "Build passed for ${{ github.sha }}"

# Public frontend availability and governed runtime sovereignty are separate concerns.
# The linked Vercel project publishes the always-on Next.js frontend from GitHub and
# provides the stable public fallback/origin. This workflow still performs no SSH
# deployment and grants no runtime authority. Owned-hardware services remain the
# governed execution substrate and should be exposed on runtime/API transport routes,
# not used as the sole availability dependency for the public homepage.
