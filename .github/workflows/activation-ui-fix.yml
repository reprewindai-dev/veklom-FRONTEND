name: Retired Activation UI helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Activation target-finality UX is committed and must be verified by canonical frontend CI."
